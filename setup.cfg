[flake8]
max-line-length = 100
exclude = .git,__pycache__,.venv,data,models,notebooks

# scripts/ と tests/ は sys.path.insert をしてから import する構造なので、
# import が先頭に来ないのは意図どおり（E402）。src/ では noqa を個別に付ける。
per-file-ignores =
    scripts/*:E402
    tests/*:E402
