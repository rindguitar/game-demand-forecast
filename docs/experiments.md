# 実験結果・検証済みの知見

実測した数字の一次ソース。**ここに書いた値を他の場所で引用する**（STATUS.md は現在地だけを持ち、数字はここを指す）。

新しい実験ほど各節の上に追記する。数字を差し替えたときは、**古い基準値と混ざらないよう「いつのどの条件か」を必ず残す**。

---

## 1. 感情分析モデルの精度

### 現行の基準値（2026-08-31・新OODセット・15シード平均）

| モデル | OOD精度 | 備考 |
|---|---|---|
| DAPT版 | **86.85%** | ドメイン適応事前学習あり |
| vanilla | **86.09%** | なし |

- DAPTの効果は **+0.76pt**。シードごとの対戦は **12勝3敗**で、方向は安定している
- OOD = 学習に使っていない未知ゲームでの評価（Issue #21 で構築）

### 旧OODセットの値（2026-08-18以前・**混ぜないこと**）

| モデル | OOD精度 | in-domain |
|---|---|---|
| DAPT版 | 88.7% | 86.6% |
| vanilla | 87.9% | — |

`get_popular_games()` が評価スコア順を返すバグ（下記 5.）の影響で偏っていたセットの値。
売上上位プール版へ差し替えたため、**現行値と直接比較できない**。

差し替えで総合精度はほぼ不変だったが、誤りの質が変わった。

### 判明している弱点

- **擁護型ポジティブレビュー**（「叩かれてるけど自分は好き」「不満はあるが良作」といった混合感情）で、ポジ側の正解率が **4〜5pt低下**する。賛否が割れるゲームで顕著。Issue #19 と地続き
- 7ゲーム学習でもOODがin-domainを上回るため、「7タイトルの語彙を覚えているだけ」という批判は数字で否定できる（ただし上の旧セットでの測定）

---

## 2. レビューの発生密度（Issue #31・2026-08-31・57ゲーム）

`scripts/benchmarks/timeseries_feasibility.py` で測定。

| 母集団 | 密度（中央値） |
|---|---|
| 累計1万件以上 | 8.37件/日 |
| 累計10万件以上 | 45.97件/日 |
| 実測最大 | 234件/日 |

- **英語フィルタ通過率は中央値 65%**。`query_summary` の件数から使える件数を見積もるときは要補正
- **`get_review_summary()` は言語フィルタを反映している**。`games.csv` の件数もポジ率も最初から英語のみ

### 中央値は5倍過小だった（2026-09-04に判明）

上の 8.37件/日 は57ゲームの**中央値**で、実際に収集する母集団（売上上位）では **208件/日・ゲーム**だった。
この誤りが「30本ないと密度が足りない」という前提を作っていたが、実測では**15本・週3,115件**（目標の3.6倍）で足りていた。

---

## 3. レビューのポジティブ率（自然比率）

| 対象 | ポジ率 |
|---|---|
| 学習7ゲーム合計 5,716,738件（全期間） | **88.8%** |
| 最高: Stardew Valley | 98.8% |
| 最低: GTA V | 83.7% |
| 時系列データセット 721,799件（直近3年） | 76.4%（期待値80.8%・乖離 −4.5pt） |

**50:50に均衡させた学習データは、ネガを実態の約4.5倍に水増ししていた**（`collect_balanced_reviews()`）。
このため、ネガ側にしか出ないトピック（crashes等）は件数を増やしても正しい比率にならない。

---

## 4. 年次季節性は検出できなかった（2026-08-31・8ゲーム×3年）

`seasonality_check.py` で測定。

- 2024 vs 2025 の**月別シェア相関 r = +0.36**。季節性と呼べる再現性は無い
- 11月に見えた山は **IdleOn の単発イベント**（通常月の4.3倍）だった。個別ゲームのイベントが全体の形を作っていた

**留保**: 完全な年が2つしか無い / 8ゲームでは個別イベントを打ち消しきれない / **週次の周期性は未測定**。

帰結: 「遡る期間は2年必要」という根拠が弱まり、期間の下限は「予測したい長さ × 3〜10」で決まることになった（→ 3年を採用）。

---

## 5. Steam API の実測で判明した挙動

- **`get_popular_games()` の `sort_by='Reviews_DESC'` はレビュー数順ではなく評価スコア順**だった（2026-08-31に発見・修正）。上位20本の累計レビュー中央値が 3,312件 → `globaltopsellers` で 79,866件に変わった
- **`get_popular_games()` は「いまの売上上位」を返す**ため、過去の断面には生存バイアスがかかる。2023年のデータは「2026年まで生き残ったゲームの2023年」しか見えない。母集団の取り方を変える手段が無いため制約として受け入れる
- **空ページを返すことがある**。連続アクセスに対しエラーではなく空ページを返し、それを「レビュー終端」と解釈すると収集が黙って切れる（→ Wiki [Silent Truncation](https://github.com/rindguitar/game-demand-forecast/wiki/Silent-Truncation)）
- **`appdetails` の `data` がリストで返るケースがある**（Eternal Return・1049590）。`get_game_genres()` がこれで落ちていた
- 販売本数・DL数は取得不可。同接は現在値のみ
- タグは `appdetails` に含まれず、**ストアページのHTMLスクレイピング**で取っている

---

## 6. 時系列データセットの点検（2026-09-04・721,799件・24ゲーム・3年）

| 項目 | 結果 |
|---|---|
| 網羅性 | **24/24 本が期間を全部カバー** |
| 自然比率 | 76.4%（期待値80.8%・乖離 −4.5pt）→ 想定内 |
| ゲーム集中 | 上位1本 19.7%（Marvel Rivals 142,497件）・上位3本 44.3% |
| 層別 | 土台14本 43.1% / 中間4本 14.4% / 直近6本 42.6% |
| ジャンル乖離 | 最大 Action +7.8pt（3ゲーム試験の +33.9pt から改善） |
| 参加ゲーム数 | 16〜24本（前回は3〜15本） |

### 土台13本なら絶対数で時系列が引ける

土台14本の週あたり件数は3年で **0.38倍**に見えるが、その半分以上は **Starfield 1本**による（発売日がウィンドウ開始と同日で、最初の26週の51.4%を占める）。

**Starfield を除く13本**: 1,736 → 1,260件/週（**0.73倍**）。週平均1,430件、最も少ない週でも1,034件。

### タグ重なりの是正（2026-09-04）

「誰と遊ぶか」を表すタグ（Multiplayer / Co-op / Online Co-Op / Local Co-Op / PvP / PvE）をノイズ扱いに追加。
候補どうしで2個以上タグがかぶる組が **13.9% → 6.5%** に減った。

---

## 7. トピック抽出

### 本番実行（2026-09-06・721,799件・学習10万件）

| 項目 | 結果 |
|---|---|
| 抽出トピック数 | 452 |
| Outlier | 312,543件（**43.3%**） |
| 所要時間 | 1,360秒（23分） |

**時系列に乗るトピック（週10件以上）**

| パネル | 割当済 | トピック数 | 言及量カバー | Outlier率 |
|---|---|---|---|---|
| 全24本 | 409,256件 | **67個** | 60.0% | 43.3% |
| 土台13本 | 135,633件 | **15個** | 40.7% | 38.8% |

**67個のゲーム集中の内訳**

| 集中度 | 個数 | 件数 | 割合 |
|---|---|---|---|
| 1ゲーム90%以上 | 16 | 68,134 | 27.8% |
| 50〜90% | 21 | 78,794 | 32.1% |
| 50%未満（複数ゲーム） | 30 | 98,593 | 40.2% |

### 試走3回の比較（2026-09-05）

| | 9,257学習 | +固有名詞除去 | 42,113学習 |
|---|---|---|---|
| 対象件数 | 20,000 | 20,000 | 100,000 |
| トピック数 | 65 | 69 | 228 |
| Outlier率 | 46.5% | 51.5% | **40.5%** |
| 1ゲーム80%以上のトピック | 23% | 19% | **26%** |
| ゲーム専用トピックに入る割合 | 38.1% | **26.6%** | 34.9% |

- 228トピックからの外挿では「全24本で78個・言及量の77%」と見込んでいたが、本番（452トピック）では **67個・60.0%** だった。**学習を増やすとトピックが細かく割れ、週10件のふるいに落ちる数が増える**
- **細かさとゲーム固有はトレードオフ**。学習を増やすとトピック数が69→228に増える一方、1ゲーム80%以上のトピックが19%→26%に増える。担い手が1本しかいない要素（デッキ構築・狩猟・宇宙探索）はロスター24本の構成上どうしても出る。**データ量では解けない**
- **Outlier率はゲームで2.5倍違う**（Once Human 60.8% ↔ theHunter 24.2%・2万件時点）。Outlierは需要として数えられないので、ゲームによって「声が届く率」が変わる

### 所要時間（fit/transform 分離）

```
学習  42,113件 →  79秒
割当 100,000件 → 157秒（件数に比例）
本番 学習10万 + 割当72万 → 1,360秒
```

見積もり（学習7分）は5倍安全側に外れていた。メモリ不足の懸念も出なかった。

---

## 8. 固有名詞の除去（2026-09-06）

判断の記録は `docs/decisions.md` 2026-09-06。ここには実測値だけ置く。

### 語だけでは固有名詞か内容語かを決められない

語ごとに「どのゲームのレビューに何回出るか」を数えた結果:

| 語 | 総出現 | 最多ゲーム | 5回以上出るゲーム数 | 判定 |
|---|---|---|---|---|
| `scrolls` | 4,005 | Starfield 98% | 6本 | 固有名詞（Elder Scrolls） |
| `scroll` | 371 | Starfield 57% | 15本 | **内容語**（スクロール） |
| `legacy` | 3,545 | GTA V 87% | 18本 | **内容語**（遺産）としても使われる |
| `song` | 2,738 | Silksong 90% | 14本 | **内容語**（曲）としても使われる |
| `cherry` | 10,653 | Silksong 98% | 12本 | — |
| `hornet` | 7,469 | Silksong 100% | 3本 | 固有名詞（キャラ名） |
| `jeff` | 6,375 | Marvel Rivals 100% | 2本 | 固有名詞（キャラ名） |
| `santos` | 215 | GTA V 100% | 1本 | 固有名詞（地名） |
| `deadpool` | 454 | Marvel Rivals 99% | 1本 | 固有名詞（キャラ名） |

**単数形と複数形で答えが割れる**（`scroll` / `scrolls`）。語の見た目だけでは判定できない。

### 一般語と紛れる1語タイトル

| 語 | 総出現 | 最多ゲーム | 意味 |
|---|---|---|---|
| `predecessor` | 1,815 | **Hollow Knight: Silksong 1,108** | 「前作」。ゲーム名ではない使われ方が多数派 |
| `starfield` | 25,807 | Starfield 25,774 | 他ゲームでの言及は33回だけ |
| `warframe` | 8,861 | Warframe 8,658 | 他ゲームでは203回 |
| `palia` | 3,549 | Palia 3,529 | 他ゲームでは20回 |
| `vrchat` | 3,109 | VRChat 3,090 | 他ゲームでは19回 |

1語タイトルを自動で全レビューから消すと `predecessor` 1,815回を失う。一方、自動から外して失うのは他ゲームでの言及275回だけ。

### 全レビューから消えていた48語（2026-09-06に是正）

ロスター24本の名前を単語に割ったもの。この中に内容語が多数含まれていた。

```
arena  auto  bladepoint  call  civilization  dave  definitive  diver
dota   edition  empires  enhanced  evolved  fantasy  fiction  final
flight forza  gathering  grand  grounded  hollow  horizon  human
knight magic  marvel  meier  microsoft  naraka  once  online  outlast
palia  plague  predecessor  rivals  silksong  simulator  split
starfield  theft  thehunter  trials  vrchat  warframe  wild  worldbox
```

- **`split`（Split Fiction）は、Split Fiction 自身に付いているSteamタグ `Split Screen` の語でもある**
- Steamタグ・ジャンルの語彙と照合すると、**48語のうち一致したのは4語だけ**（`fantasy` `flight` `grand` `split`）。ロスター24本のタグは `MOBA` `Card Game` `Open World` のような短い語彙で、`magic` や `civilization` を含まない → **タグ語彙はホワイトリストとして使えない**
- `configs/proper_nouns.txt` の46語は同じ照合で**衝突ゼロ**。手動リストは内容語を巻き添えにしていなかった

### 除去の効果（2026-09-05・`bf83f53`）

| 指標 | 除去なし | 除去あり |
|---|---|---|
| ゲーム専用トピックに入るレビュー | 38.1% | **26.6%** |
| 1ゲーム80%以上のトピック | 15本 | 13本 |
| 集中度の中央値 | 44.9% | 41.4% |
| Outlier率 | 46.5% | 51.5%（情報を減らした分だけ増える） |

**注意**: この比較はロスター名の単語分割と `proper_nouns.txt` 46語を**同時に入れた**ときの数字で、寄与が分けられていない。46語は無罪と分かったので、次に測るときは2つを分けること。

---

## 9. 旧データ（`reviews_10000.csv`）が時系列に使えない理由

2026-08-18に実測。Phase 7 で別途収集する判断（`docs/decisions.md`）の根拠。

- **密度不足**: 5,605件 ÷ 86トピック ÷ 549日 = **1トピック1日あたり0.12件**。表のほとんどが0になる
- **自然比率の欠如**: `collect_balanced_reviews()` がPos/Negを50:50に強制しているため、ネガ側にしか出ないトピックが水増しされている。**件数を増やしても直らない**
- **期間がバラバラ**: 件数固定で集めたため、CS2は3日分・Stardew Valleyは190日分と時間軸が揃っていない

---

## 10. 一律圧縮は誤りだった（2026-08-31）

確定仕様（30本・週次）で週あたり998件（トピック付き）。

```
トピック86個のまま → 平均 11.6件/週  足りる
ただし実際の分布は偏る（最大6.1% / 中央値0.9% / 最小0.4%）
  → 中央値のトピックは 8.6件/週（不足）
  → 最小のトピックは   3.6件/週（大幅に不足）
```

**一律に15〜20個へ圧縮する必要はない。密度が足りないのは小さいトピックだけ。**
それまで「15〜20個に束ねる」と繰り返していたのは平均だけを見た誤りだった。
