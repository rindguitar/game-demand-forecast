# STATUS

最終更新: 2026-09-05（セッション終了時に必ず更新）

## 現在のフェーズ

**Phase 5: データ蓄積・分析**（当初予定表 Week 7-8）。

当初「Phase 7（Prophet実装）に入るところ」と認識していたが、予定表と実態を突き合わせた結果、**Phase 5 と 6 が丸ごと未着手**であることが判明した。Phase 3（感情分析）を予定表の想定（既製モデルをロードするだけ）から大きく踏み込んで実装したため、深さは出た一方で Phase 5 以降が空いている。

依存関係: Phase 2 の「自動収集スクリプト（日次実行）」が未実装 → Phase 5 の継続的データ収集ができない → Phase 6 の時系列整形ができない → Phase 7 の Prophet に届かない。**積み残し1つが3フェーズを止めている。**

**2026-09-05時点**: Issue #31 完了・#32 の収集まで完了。**本収集が完走**（24ゲーム・721,799件・全ゲームで3年分をカバー）。Phase 5 のデータ蓄積は終わり、**次はトピック抽出（Phase 6 の入口）**。

なお Phase 5 の「継続的データ収集（最低1ヶ月分）」は**1ヶ月待つ必要はない**。Steamレビューは `timestamp_created` を持ち古い方へ遡れるため、過去分をまとめて取得できる（Reddit想定の記述と思われる）。

## 前回やったこと

### 2026-09-04〜05: 収集の作り直しと本収集の完走

**本収集が完走した。24ゲーム・721,799件・24本すべてが3年分をカバー**（`data/timeseries/`）。ただしそこに至るまでに、収集と選定の両方に構造的な欠陥が見つかった。

- **収集が黙って途中で切れていた（最重要）**。Steamは連続アクセスに対しエラーではなく**空ページ**を返すことがあり、それを「レビュー終端」と解釈して打ち切っていた。前回の15本のうち**7本が期間の途中で切れ、3本は1件も取れていなかった**（World of Warships は3年で11,799件取れるはずが1,664件＝14%）。既存の警告は「上限40万件に当たったか」しか見ておらず検出できなかった
  - 対策: 空ページは同じcursorで待ってやり直す（直前が満杯のページならレート制限を疑って5s→15s→45s）／停止理由を返す／失敗しても取れた分は捨てない／**発売日と突き合わせて網羅性を判定**し `collection_log.csv` に記録／再開時にスキップするのは完全に取れた本だけ
  - 網羅性判定の猶予は**7日**。30日だと早期アクセス作品（appdetailsが1.0の日付を返す）を「取り切った」と誤判定する（Hades II で実証）
- **ゲーム選定が算数として30本に届かない設計だった**。候補297本・1本あたり平均2.39ジャンル・9ジャンルなので、30本には72枠必要なのに `max_per_genre=6` では54枠しか作れない。さらに上限は「多すぎ」しか止めないため、Sports 1本・Racing 2本という状態を防げていなかった
  - **ジャンルは上限ではなく下限（各3本以上）**に変更。希少なジャンルから先に埋める
  - **土台（発売がウィンドウ開始より前）は上限14本**。下限にすると土台を探しに行って新しい側の枠を食う（実測で直近2年の発売が3本まで減った）。二択なので「土台の上限14」＝「新しい側の下限10」で、数字1つで両方決まる
  - 母集団は「1万件以上（英語）かつ**発売から1年以上**」。履歴が短いゲームは時系列に寄与しない
  - ジャンル下限は候補全部のジャンルを先に知る必要があるため、**メタ情報を `pool_cache.json` に集めてから選ぶ**方式に変更。2回目以降はAPIを叩かず、`--dry-run` で条件を変えた選び直しが数秒で回る。台帳があれば選定はやり直さない（`--reselect` のときだけ）
- **タグ重なり判定のノイズタグに「誰と遊ぶか」を追加**（Multiplayer / Co-op / Online Co-Op / Local Co-Op / PvP / PvE）。`Singleplayer` だけが除外され `Multiplayer`（候補の49.0%）が残っていたため、多人数ゲームばかりが「似ている」と判定されていた。候補どうしで2個以上タグがかぶる組は 13.9% → 6.5% に

**前提の誤りが2つ判明した**

- **密度の見積もりが5倍過小だった**。#31 の「1万件以上で8.37件/日」は57ゲームの**中央値**で、実際の母集団（売上上位）は**208件/日・ゲーム**。「30本ないと密度が足りない」という前提が消えた（実測で15本・週3,115件、目標の3.6倍）
- **`get_review_summary()` は言語フィルタを反映していた**。`games.csv` の件数もポジ率も最初から英語のみで、点検の期待値との差の理由は「期間（直近3年 vs 全期間）」だけだった

**点検結果（721,799件）**

| 項目 | 結果 |
|---|---|
| 網羅性 | **24/24 本が期間を全部カバー** |
| 自然比率 | 76.4%（期待値80.8%・乖離 −4.5pt）→ 想定内 |
| ゲーム集中 | 上位1本 19.7%（Marvel Rivals 142,497件）・上位3本 44.3% |
| 層別 | 土台14本 43.1% / 中間4本 14.4% / 直近6本 42.6% |
| ジャンル乖離 | 最大 Action +7.8pt（3ゲーム試験の +33.9pt から改善） |
| 参加ゲーム数 | 16〜24本（前回は3〜15本） |

**土台13本で絶対数の時系列が引ける**。土台14本の週あたり件数は3年で0.38倍に見えるが、その半分以上は Starfield 1本（発売日がウィンドウ開始と同日で、最初の26週の51.4%を占める）。**Starfield を除く13本なら 1,736 → 1,260件/週（0.73倍）で、週平均1,430件・最も少ない週でも1,034件（86トピックで12.0件/トピック）**。8月31日に「Y軸はシェア主軸」へ倒した理由（参加ゲームの入れ替わり）は、パネルを分けることで解けた。

Wikiに2ページ追加（[収集が途中で切れていることに気づく](https://github.com/rindguitar/game-demand-forecast/wiki/Silent-Truncation) / [パネル設計](https://github.com/rindguitar/game-demand-forecast/wiki/Panel-Design)）、トピック抽出に「学習と当てはめを分ける」節を追加、ドキュメントマップを42ページ86本で再計算。

### 2026-09-02: 収集の実行と周辺整備

- `scripts/collect/inspect_timeseries_dataset.py` を追加（偏り点検・予定表 Phase 5「データ品質レポート」に相当）
- 収集スクリプトが選定時のジャンル・タグを捨てていたので、`data/timeseries/games.csv` に台帳として保存するよう変更
- **収集上限で打ち切られたゲームを検出・警告**するよう追加。既定値も 20万 → 40万 に引き上げ（#31 の実測最大 234件/日 × 3年 = 約26万件で、20万では確実に当たっていた）
- make コマンド追加（`timeseries-dataset` / `collect-timeseries` / `inspect-timeseries`）
- **バグ修正**: `get_game_genres()` が appdetails の `data` がリストで返るケースで落ちていた。売上上位プール497本を走査して該当1本（Eternal Return・1049590）を特定。本収集がこれで停止していた

### 2026-08-31: Issue #31 完了・#32 実装

- **#31（レビュー発生密度の実測）完了・クローズ**。`scripts/benchmarks/timeseries_feasibility.py` で57ゲームを測定。累計1万件以上のゲームで 8.37件/日、10万件以上で 45.97件/日、英語フィルタ通過率は中央値65%
- 作業中に **`get_popular_games()` のバグを発見・修正**。`sort_by='Reviews_DESC'` はレビュー数ではなく評価スコア順だった（上位20本の累計レビュー中央値が 3,312件 → `globaltopsellers` で 79,866件）
- そのバグの影響で偏っていた **OODテストセットを差し替え**。新基準値は DAPT 86.85% / vanilla 86.09%（15シード）。総合精度はほぼ不変だが**擁護型ポジティブレビューという弱点**が判明（→ #19 にコメント）
- **年次季節性を測定 → 検出できず**（`seasonality_check.py`・2024 vs 2025 の月別シェア相関 r=+0.36）。11月の山は個別ゲームの単発イベントだった
- **#32 の仕様を全て確定**（週次 / 1万件以上30本 / 3年 / シェア主軸 / タグで束ねる / 本文を保存）
- **#32 を実装**。`steam_collector.py` に3つ追加（`since_ts`・`detailed`・`collect_natural_reviews`・`get_review_summary`）、`scripts/collect/collect_timeseries_dataset.py` を新規作成。小規模試験で期間打ち切り・18列取得・自然比率を検証済み

### 2026-08-27: ドキュメントの図解（Phase進行とは独立）

- `scripts/` `src/` `tests/` のREADMEに、スクリプト・モジュール・テストの繋がりを示すMermaid図を追加（計14枚）
- Wikiに[ドキュメントマップ](https://github.com/rindguitar/game-demand-forecast/wiki/Documentation-Map)を新規作成。全38ページの相互リンクを実測し、密に繋がるページをブロックにまとめて階層化
- 図の作成手順を `docs/mermaid-guide.md` に残した（読みにくさの指摘で3回作り直したため）
- 不要ブランチを整理（ローカル11本・リモート1本削除。`main` と `feature/sentiment-db-visualization` のみ残す）

**図を作る過程で判明したコードの実態**（未対応・下記「未解決」に転記）

### 2026-08-18まで

- LTでの指摘「なぜ7ゲーム10000件なのか。多ゲーム・少レビューでは？」を受けて Issue #30 を作成
- その過程で、未知ゲームでの評価（OOD）は Issue #21 で構築済みと確認。**7ゲーム学習でも OOD 87.9%**（15シード平均・DAPT版88.7%）で、in-domain の86.6%を上回る。「7タイトルの語彙を覚えているだけ」は数字で否定できる
  - ただしこの15シード平均は**差し替え前のOODセット**で測ったもの。**現行の基準値は新セット15シードの DAPT 86.85% / vanilla 86.09%**（下記「未解決」参照）
- Phase 7 の前提確認で、既存 `reviews_10000.csv` が時系列に使えないことを実測で確認（下記）
- Issue #31（調査）/ #32（実装）を作成。Wiki に解説2本を追加

## 次の一手（優先順）

1. **トピック抽出の規模対応**（着手前に判断が要る）

   72万件をそのまま `extract_topics.py` に渡すと、BERTopicの次元削減（UMAP）で**メモリ不足になる可能性が高い**（RAM 16.7GB・空き14.5GB / 入力284MB・埋め込みだけで1.1GB）。方針は合意済みで未実装:

   - `--fit-sample-size` / `--sample-per-game` を追加し、**一部で学習（fit）→ 全件に当てはめ（transform）**に分ける
   - サンプルが決めるのは**トピックの顔ぶれだけ**で、件数は全件から出る。したがってサンプルは**ゲームごとに均等**に取る（単純ランダムだと Marvel Rivals の19.7%がそのままトピック構成に出る）
   - `min_topic_size` の直書き（20）を引数化する
   - 収集済みデータには英語フィルタを掛けない（収集時に通過済みなので二重）
   - 学習済みモデルを保存し、再実行時に再学習しない

   実装したら**2万件で1回通して**所要時間とトピックの手触りを見る。そこで `min_topic_size` の当たりを付ける。

2. **粒度の判定** — 「週10件を下回るトピックが何個あるか」を実測する。旧データの86トピックは捨てるデータの産物なので判断材料にしない
3. **PRを出してマージ** — `feature/timeseries-collector` に10コミット溜まっている（PR未作成）
4. Phase 6（時系列データ整形）→ Phase 7（Prophet実装）
   - 時系列の主軸パネルは**土台13本（Starfield除く）**。絶対数で引ける
   - 全24本は話題の発見に使う。**どのトピックが時系列で追えて、どれが追えないか**はパネルの構成で決まる

### 再収集が必要になったときのコマンド

```bash
make collect-timeseries      # 台帳があれば選定はやり直さない。未完了の本だけ収集
make inspect-timeseries      # 偏り点検（収集途中でも可）

# 条件を変えて顔ぶれだけ見る（APIを叩かないので数秒）
docker compose exec dev python scripts/collect/collect_timeseries_dataset.py \
    --dry-run --reselect --max-backbone 12
```

`data/timeseries/pool_cache.json`（母集団497本のメタ情報）は消さないこと。消すと選定に15〜20分かかる。
`reviews_timeseries.csv` は**唯一の再現不能な資産**（レビュー本文があるので、トピックの作り方を変えても遡って作り直せる）。

## 未解決・保留中の問題

- **`extract_topics.py` が72万件に対応していない**。`min_topic_size = 20` がスクリプトに直書き（10000件向けの値）／UMAP・HDBSCANのパラメータを指定しておらず既定値のまま／収集済みデータに英語フィルタを二重で掛けている。→ 次の一手1で対応
- **Marvel Rivals が全体の19.7%**（142,497件）。発売スパイクなので、トピックの時系列がこの1本の launch に引きずられうる。層別では直近6本で42.6%
- **Starfield の発売スパイクが土台の期間の頭にある**。土台14本の減少（0.38倍）の半分以上がこれ。**時系列の主軸パネルからは外す**方針（データは残すので分析時のフィルタで済む）
- **全24本では参加ゲーム数が16〜24本で変動する**。絶対数を使うのは土台13本のパネルに限り、全24本を見るときはシェアを使う
- ~~既存データが時系列に使えない理由（実測値）~~ → **解消済み**。以下は旧データ（reviews_10000.csv）についての記録
  - 密度不足: 5,605件 ÷ 86トピック ÷ 549日 = **1トピック1日あたり0.12件**。表のほとんどが0になる
  - 自然比率の欠如: `collect_balanced_reviews()` がPos/Negを50:50に強制しているため、ネガ側にしか出ないトピック（crashes等）が水増しされている。**件数を増やしても直らない**
  - 期間バラバラ: 件数固定で集めたため CS2は3日分・Stardew Valleyは190日分と、時間軸が揃っていない
- Outlier率 43.2%（トピック分類不能）。時系列に使えるのは半分強になる。Issue #15（トピック抽出の品質改善）はクローズ済みのため、Outlierを減らすか許容するかは Issue #32 の中で判断する
- **トピックの目標粒度（候補1の物差し）が未決定**。「次に作るゲームの企画を書くとして、どのくらいの粗さの情報があれば足りるか」を1回だけ決める必要がある。**ただし本収集→トピック抽出の後に、実データを見てから決める方針**（旧データの86トピックは捨てるデータの産物なので判断材料にならない）
- ~~ゲーム選定が18本で止まった（目標30本）~~ → **解決**（2026-09-04）。原因はジャンル上限と目標本数の算数的な衝突。下限＋土台上限に作り替えて24本を選定。密度も「30本必要」の前提自体が誤りだった
- **ジャンル均衡は「本数」で担保し、量の偏りは点検で見る方針に確定**。実測の乖離は最大 Action +7.8pt（本数19.0% vs 量26.9%）で、3ゲーム試験の +33.9pt からは大きく改善した。量ベースの均衡に変えるのは「需要スコアは実数合算」の決定と矛盾するため採らない
- **レビュー数 = プレイヤー数 × 書く傾向**。賛否が割れるゲームは1人あたりのレビュー率が高く、レビュー爆撃も混ざる。需要の代理指標としてのノイズ
- **大作のリリース時期がトレンドを作る**。ELDEN RING のレビューは3年間に均等ではなく発売・DLC時に集中するため、時系列の山が「需要の変化」ではなく「発売スケジュール」を映す可能性がある。シェアを選んだことで部分的に緩和されるが消えはしない
- 需要の測定が供給に引きずられる構造的な限界がある。人は「あるもの」について語り「無いもの」については語らないため、未充足需要は最も観測されにくい。タグによる需要／供給のギャップ分析はこの分だけ圧縮されて見える
- `get_game_tags()` は公式APIではなく**ストアページのHTMLスクレイピング**（`appdetails` にタグは含まれないため）。Steam側のHTML変更で壊れる。供給側の測定と検証がこれに依存している
- **`get_popular_games()` は「いまの売上上位」なので、過去の断面には生存バイアスがかかる**。2023年のデータは「2026年まで生き残ったゲームの2023年」しか見えない。母集団の取り方を変える手段が無いため、制約として受け入れる
- ~~「Steamの実レビューはポジティブが7〜9割」は未検証~~ → **実測済み**。学習7ゲーム合計 5,716,738件で**ポジ率 88.8%**（最高 Stardew Valley 98.8% / 最低 GTA V 83.7%）。50:50 に均衡させた学習データは、ネガを実態の約4.5倍に水増ししていた
- ~~Issue #31 のレビュー発生密度は未測定~~ → **実測済み**（2026-08-31・57ゲーム）。1万件以上のゲームで 8.37件/日、10万件以上で 45.97件/日。→ #32 の粒度は「週次 × トピック15〜20 × 累計1万件以上を30本程度」が現実的
- 英語フィルタ通過率は中央値 **65%**。`query_summary` の件数から使える件数を見積もるときは要補正
- ~~`seed_study` の15シード結果が旧OODセット基準~~ → **測り直し完了**（2026-08-31）。**基準値は DAPT 86.85% / vanilla 86.09%**（新セット・15シード平均）。DAPTの優位は +0.79 → +0.76pt でほぼ同一、シード対戦は12勝3敗。旧セットの 88.7% / 87.9% とは**混在させないこと**
- **年次の季節性は検出できなかった**（2026-08-31・8ゲーム×3年）。2024 vs 2025 の月別シェア相関 r=+0.36。11月の山は IdleOn の単発イベント（通常月の4.3倍）だった。→ 「2年必要」の根拠が弱まり、期間の下限は「予測したい長さ×3〜10」で決まる
  - 留保: 完全な年が2つのみ・7ゲームでは個別イベントを打ち消しきれない・**週次の周期性は未測定**
- **新たに見つかったモデルの弱点**: 賛否が割れるゲームの「擁護型ポジティブレビュー」（叩かれてるけど自分は好き・混合感情）でポジ正解率が 4〜5pt 低下。Issue #19 と地続き
- 予定表（Phase 0）の `docs/requirements.md` / `docs/design.md` が23週間未作成。需要スコアの定義が固まってきたので、書ける材料は揃いつつある
- Reddit の採用中止が decisions.md に未記録（`requirements.txt` の1行コメントのみが根拠）
- Issue #30（多ゲーム・少レビュー）は時系列フェーズが一区切りしてから着手
- **`src/visualization/sentiment_plots.py` が実行できない**。存在しない `src/nlp/sentiment_db.py` をimportしている。どのスクリプトからも呼ばれていないため実害は出ていないが、`feature/sentiment-db-visualization` ブランチと関係する可能性あり（2026-08-27に図解の過程で発見）
- **`src/data/dataset_split.py` がどのスクリプトからも呼ばれていない**。`train_sentiment.py` は自前で `train_test_split` を呼んでいる（同上）
- **`src/nlp/` の中核3ファイル（`model.py` / `train.py` / `dataset.py`）にテストが無い**（同上）

## 直近の決定事項

- 2026-09-04: 「誰と遊ぶか」を表すタグは似ている判定から外す（既存基準の適用漏れの修正）→ 詳細は docs/decisions.md
- 2026-09-04: ゲーム選定を「ジャンル下限3本 ＋ 土台上限14本」に変更。上限は最悪ケースを縛る道具、下限はカバレッジを保証する道具として使い分ける
- 2026-09-04: 土台は**下限ではなく上限**で指定する。下限にすると探しに行って新しい側の枠を食う。二択なので数字1つで両方が決まる
- 2026-09-04: 選定条件を確定（24本 / ジャンル下限3 / 土台上限14 / タグ重なり2 / ジャンル上限12 / 履歴1年以上）
- 2026-09-04: 母集団のメタ情報をキャッシュしてから選ぶ方式にする。選定が再現可能になり、条件を変えた検討がAPI無しで回る
- 2026-09-05: 時系列の主軸パネルは土台13本（Starfield除く）。絶対数で引ける。全24本はトピックの発見に使う
- 2026-08-18: Phase 7 は既存データを使わず時系列用に別途収集する → 詳細は docs/decisions.md
- 2026-08-18: 収集の実測（#31）を実装（#32）より先に行う → 同上
- 2026-08-18: SQLite DB（Issue #10）は現在の規模では不要と判断し保留 → 同上
- 2026-08-18: 需要スコアの骨格を決定。掛け算にせず「需要の大きさ（言及数）」と「充足度（ポジ率）」を別軸にする → 同上
- 2026-08-18: 不満を「満たされていない需要」と「満たされていない前提（不具合・運営）」に仕分ける → 同上
- 2026-08-18: プレイ時間は需要の強さの重みには使わない（離脱者の声が消えるため）→ 同上
- 2026-08-18: 販売本数・DL数はSteam APIで取得不可、同接も現在値のみと実測確認。需要スコアは検証不能な指標として扱う → 同上
- 2026-08-18: 需要スコアは**ゲーム要素（トピック）単位・実数合算**。帰結として Issue #32 のゲーム選定が定義の一部になる → 同上
- 2026-08-18: トピックは**3分類**（要素 / 品質・運営 / ビジネス条件）。仕分けは ルール→手動→分類器 → 同上
- 2026-08-18: 成果物は**株価チャート型**。Y軸は言及数（予測精度を検証できるようにするため）→ 同上
- 2026-08-18: 充足度の配色は**オレンジ ↔ アクア**。赤↔緑は色覚多様性で識別不能のため却下 → 同上
- 2026-08-18: **同接の定期記録を始める**（タグが粒度の橋渡しになるため、前回の「不要」判断を撤回）→ 同上
- 2026-08-18: #31 の測定設計を確定（対象55本・平均レート＋4本のみ日次分布・フィルタ前後を記録）→ 同上
- 2026-08-31: `get_popular_games()` が評価スコア順を返していたのを売上上位順に修正 → 同上
- 2026-08-31: OODテストセットを売上上位プール版に差し替え（総合精度はほぼ不変・誤りの質は変化）→ 同上
- 2026-08-31: 新OODセットで seed_study を測り直し。DAPTの効果は +0.76pt で再現 → 同上
- 2026-08-31: 年次季節性は検出できず。遡る期間の「2年必須」根拠が弱まった → 同上
- 2026-08-31: Y軸は**シェア主軸＋総言及数を補助線**。参加ゲームの入れ替わりで絶対数だと偽の成長が出るため → 同上
- 2026-08-31: 遡る期間は**3年**。2年だと年どうしの比較が1組しか取れず偶然を排除できない → 同上
- 2026-08-31: トピックの束ね方は**1と2で目標を決め、4（Steamタグ）で実行**。3は使わない（タグ対応を壊すため）→ 同上
- 2026-08-31: **一律圧縮は誤り**と判明。86トピックでも平均11.6件/週で足りる。手を入れるのは小さいトピックのみ → 同上
- 2026-08-31: 要素の強弱は時系列ではなく**断面**で調べる。#32 に「レビュー本文を保存」の要件を追加 → 同上
- 2026-08-31: 大作が需要スコアで重くなるのは**設計通り**（絶対量として需要を定義した帰結）。ただしジャンル均衡が本数のみである点は別問題として要確認
- 2026-08-27: 図は必ず画像に描き出して目視してから出す（構文チェックだけでは貫通を検出できない）→ 同上
- 2026-08-27: 密な図は「分ける」より「まとめる」で直す（分割は全体像を失う）→ 同上
- 2026-08-27: マップの生成スクリプトは作らず、手順書 `docs/mermaid-guide.md` だけ残す → 同上
